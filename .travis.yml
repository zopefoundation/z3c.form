sudo: false
language: python
python:
    - 2.7
    - 3.3
    - 3.4
    - 3.5
install:
    - pip install -U zc.buildout
    - buildout bootstrap
    - buildout
script:
    - coverage run bin/test
after_success:
    - coveralls
notifications:
    email: false
cache: pip
